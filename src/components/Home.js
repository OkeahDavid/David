import React from 'react';
import { Box, Typography, Button } from '@mui/material';

const Home = () => {
  return (
    <Box>
      <Typography variant="h1">Hi, I'm David</Typography>
      <Typography variant="h2">I make stuff ...sometimes.</Typography>
      <Button variant="contained">Say Hi!</Button>
    </Box>
  );
};

export default Home;
